# 937 CMS 环境变量配置示例

# ============ API 服务器配置 ============
# 本地开发：复制 apps/api/.env.example 到 apps/api/.env
# 线上部署：复制 apps/api/.env.production 到 apps/api/.env 并修改配置

# ============ Admin 前端配置 ============
# 本地开发：复制 apps/admin/.env.example 到 apps/admin/.env
# 线上部署：复制 apps/admin/.env.production 到 apps/admin/.env 并修改配置

# ============ 快速开始 ============
# 1. 本地开发环境：
#    cd apps/api && cp .env.example .env
#    cd ../admin && cp .env.example .env
#
# 2. 线上部署环境：
#    cd apps/api && cp .env.production .env && vim .env (修改数据库/Redis地址)
#    cd ../admin && cp .env.production .env && vim .env (修改API地址)
#
# 3. 启动服务：
#    pnpm install
#    pnpm build
#    pnpm start
