# 服务器配置
PORT=3000

# 数据目录（可选，默认为 ./data）
CMS_DATA_DIR=

# 初始化后自动重启（可选）
CMS_AUTO_RESTART_AFTER_SETUP=false

# 路由别名配置（安全性增强，仍需配合认证/限流使用）
# 公共 API 实现在 /api/v1/*，可通过此配置暴露为 /api/public/* 供模板使用
CMS_PUBLIC_API_PREFIX=/api/public
CMS_PUBLIC_API_DISABLE_V1=false
# 管理 API 实现在 /admin/*，可自定义前缀并选择性禁用默认的 /admin/*
CMS_ADMIN_API_PREFIX=/admin
CMS_ADMIN_API_DISABLE_DEFAULT=false

# 数据库配置（运行时使用）
MYSQL_HOST=127.0.0.1
MYSQL_PORT=3306
MYSQL_DATABASE=bb_cms
MYSQL_USER=root
MYSQL_PASSWORD=123456
